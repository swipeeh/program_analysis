public class AST {

    // This class should define the AST structure  so that we can at least perform some operations on it.
    // Some find it easier to implement the AST data structure first, then work on the parser afterwards.

    // Constructor
    public AST() {

    }



}
